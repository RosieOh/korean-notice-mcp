# 데이터 처리와 보안 범위

- 로컬 stdio 전용, 읽기 전용 서버입니다. HTTP 서버·인증·다중 사용자 격리는 없습니다.
- 도구는 외부 네트워크에 접속하지 않습니다. 문서 내용이 어디로 전송되는지는 연결한 AI 앱과 모델 제공자의 정책을 따릅니다.
- `MCP_DATA_DIR`이 신뢰 경계입니다. 경로를 realpath로 확인해 폴더 밖 접근과 심볼릭 링크 우회를 거부합니다. 검사와 읽기 사이에 파일을 바꿔치는 악의적 로컬 프로세스까지는 막지 않으므로, 전용 입력 폴더를 쓰세요.
- 원본을 수정하지 않고 원문을 디스크나 로그에 남기지 않습니다. stdout에는 MCP 프로토콜 메시지 또는 `--demo` 결과만 씁니다.
- 제한: 파일 2MB, HWPX 압축 해제 8MB·엔트리 512개·3초, rhwp 파싱 10초·메모리 128MB. HWPX의 DTD와 사용자 엔티티는 거부합니다.
- 문서 안의 지시문은 텍스트로만 반환합니다. 실행·파일 쓰기·URL 조회 도구는 없습니다. AI 앱이 공고 원문을 명령으로 취급하지 않도록 주의하세요.
- 추출 결과는 검토용 후보입니다. 신청 자격이나 제출 의무를 확정하지 않습니다.

개인정보가 들어간 실제 신청 서류를 이슈에 첨부하지 마세요. 공개 공고의 링크나 최소 합성 파일로 재현해 주세요. 보안 문제는 공개 이슈 대신 GitHub의 비공개 보안 제보(Security advisories)로 알려 주세요.
